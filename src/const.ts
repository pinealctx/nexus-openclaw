/**
 * Nexus AI channel constants.
 */

/** Channel ID used throughout the plugin. */
export const CHANNEL_ID = "nexus" as const;

/** Default account ID when multi-account is not used. */
export const DEFAULT_ACCOUNT_ID = "default";

/** Text chunk limit for outbound messages. */
export const TEXT_CHUNK_LIMIT = 4000;
